import React from "react";
import LoginCard from "../features/auth/components/loginCard";

export default function LoginPage() {
  return (
    <div className="flex items-center bg-[#15191d] justify-center h-screen">
      <LoginCard />
    </div>
  );
}
